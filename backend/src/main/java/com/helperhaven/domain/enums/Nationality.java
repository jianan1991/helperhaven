package com.helperhaven.domain.enums;

/** Country of origin for helper profiles. Mirrors the Postgres `nationality_t` enum. */
public enum Nationality {
    IDN,   // Indonesia
    MMR,   // Myanmar
    PHL,   // Philippines
    OTHER
}
