spring:
  jpa:
    properties:
      hibernate:
        format_sql: false

logging:
  level:
    root: INFO
    com.helperhaven: INFO

helperhaven:
  storage:
    provider: s3
    endpoint: ""
    path-style-access: false
