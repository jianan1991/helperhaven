# Copy to .env and edit locally. NEVER commit the real .env.

# --- Database ---
DB_NAME=helperhaven
DB_USER=helperhaven
DB_PASSWORD=helperhaven

# --- Object storage (MinIO locally, S3 in prod) ---
STORAGE_ACCESS_KEY=minioadmin
STORAGE_SECRET_KEY=minioadmin
STORAGE_BUCKET=helperhaven-dev

# --- Auth ---
JWT_SECRET=dev-jwt-secret-please-change-in-prod-dev-jwt-secret-please-change

# --- Stripe (use test keys locally) ---
STRIPE_SECRET_KEY=sk_test_replace_me
STRIPE_WEBHOOK_SECRET=whsec_replace_me

# --- KYC provider (fill when integrated) ---
KYC_PROVIDER=SUMSUB
KYC_API_KEY=
KYC_API_SECRET=
